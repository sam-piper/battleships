namespace Battleships.Model
{
    /// <summary>
    /// Defines board orientation states for a ship model.
    /// </summary>
    public enum ShipOrientation
    {
        Horizontal = 0,
        Vertical = 1
    }
}